### 3월 목표

1. 구현 문제 20개 이상 풀고..
2. 알고리즘 [기본 유형](https://www.acmicpc.net/group/workbook/11922) 문제 10개 이상 풀기
3. 매일 문제 풀기
4. 한번에 정답 맞추기 위해 **엣지케이스 만들어서 검증하기**
5. 설계하는 연습하기
6. 코드 퀄리티 신경쓰지말고 일단 돌아가는 코드 빠르게 작성하기

</br>

| 완전탐색     | 그리디 | DFS/BFS      | 투포인터     | 빡구현 |
| ------------ | ------ | ------------ | ------------ | ------ |
| 7            | 6      | 17           | 3            | 3      |
| **이분탐색** | **DP** | **분할정복** | **최단거리** |
| 0            | 0      | 0            | 0            |

</br>

### 아맞다

#### 이차원 배열 복사하기

```js
const new_arr = arr.map((row) => [...row]);
```

#### 이차원 배열 자르기

```js
board.slice(start_m, end_m).map((row) => row.slice(start_n, end_n));
```

#### 배열 요소의 합

```js
const sum = arr.reduce((a, b) => a + b);
```

#### some, every 활용하기

```js
arr.some((value) => 조건); // 하나라도 만족하면 true
arr.every((value) => 조건); // 모두 만족해야 true
```

#### 순열/조합 30초, 우선순위큐 3분 안에 구현하기
