### 📖 풀이한 문제
- 백준 2910 - 빈도 정렬
- 문제 링크: https://www.acmicpc.net/problem/2910
- 난이도: 실버3
- 걸린 시간: 10분

### 🧩 풀이 설계 / 막힌 부분
- 처음 접근 아이디어: Map으로 푼다.
- 고려한 시간복잡도: 정렬 O(N logN)
- 막힌 부분 / 해결 방법:

### ⭐️ 문제에서 주로 사용한 알고리즘
-

### 🧠 대략적인 코드 설명
```js
const input = require("fs")
    .readFileSync("dev/stdin")
    .toString()
    .trim()
    .split("\n");

const [n, c] = input[0].split(' ').map(Number);
const arr = input[1].split(' ').map(Number);

const mp = new Map();
for (let i = 0; i < n; i++) {
    mp.set(arr[i], (mp.get(arr[i]) || 0) + 1);
}

const nums = [...mp.entries()];
nums.sort((a, b) => b[1] - a[1]);

const ans  = [];
for(let i = 0;i < nums.length;i++) {
    for(let j = 0;j < nums[i][1];j++) {
        ans.push(nums[i][0]);
    }
}

console.log(ans.join(' '));
```
