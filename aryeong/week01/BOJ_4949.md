### 📖 풀이한 문제
- 백준 4949 - 균형잡힌 세상
- 문제 링크: https://www.acmicpc.net/problem/4949
- 난이도: 실버4
- 걸린 시간: 15분

### 🧩 풀이 설계 / 막힌 부분
- 처음 접근 아이디어: 여는 괄호는 stack에 push. 닫는 괄호는 stack의 마지막 괄호와 비교해서 짝이 안 맞으면 no
- 고려한 시간복잡도: O(NM)
- 막힌 부분 / 해결 방법:

### ⭐️ 문제에서 주로 사용한 알고리즘
-

### 🧠 대략적인 코드 설명
```js
function solution(str) {
    const pairs = {
        ")": "(",
        "]": "["
    };
    const stack = [];

    for (let i = 0; i < str.length; i++) {
        if (str[i] === "(" || str[i] === "[") stack.push(str[i]);
        else if (pairs[str[i]]) {
            if(stack.pop() !== pairs[str[i]]) return false;
        }
    }

    return stack.length === 0;
}


for (let i = 0; i < input.length - 1; i++) {
    console.log(solution(input[i]) ? "yes" : "no");
}
```
